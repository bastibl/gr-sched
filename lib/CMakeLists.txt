# Copyright 2011,2012,2016,2018,2019 Free Software Foundation, Inc.
#
# This file was generated by gr_modtool, a tool from the GNU Radio framework
# This file is a part of gr-sched
#
# SPDX-License-Identifier: GPL-3.0-or-later
#

########################################################################
# Setup library
########################################################################
include(GrPlatform) #define LIB_SUFFIX

include (FindLTTngUST REQUIRED)

include_directories(.)
# add_library(tracepoint-provider STATIC tp.cpp)
# target_link_libraries(tracepoint-provider LTTng::UST)

list(APPEND sched_sources
    copy_rand_impl.cc
    msg_forward_impl.cc
    ncopy_impl.cc
    null_sink_latency_impl.cc
    null_source_latency_impl.cc
    tp.cpp
)

set(sched_sources "${sched_sources}" PARENT_SCOPE)
if(NOT sched_sources)
    MESSAGE(STATUS "No C++ sources... skipping lib/")
    return()
endif(NOT sched_sources)

add_library(gnuradio-sched SHARED ${sched_sources})
target_link_libraries(gnuradio-sched
    gnuradio::gnuradio-runtime
    LTTng::UST
  )
target_include_directories(gnuradio-sched
    PUBLIC $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../include>
    PUBLIC $<INSTALL_INTERFACE:include>
  )
set_target_properties(gnuradio-sched PROPERTIES DEFINE_SYMBOL "gnuradio_sched_EXPORTS")

if(APPLE)
    set_target_properties(gnuradio-sched PROPERTIES
        INSTALL_NAME_DIR "${CMAKE_INSTALL_PREFIX}/lib"
    )
endif(APPLE)

########################################################################
# Install built library files
########################################################################
include(GrMiscUtils)
GR_LIBRARY_FOO(gnuradio-sched)

########################################################################
# Print summary
########################################################################
message(STATUS "Using install prefix: ${CMAKE_INSTALL_PREFIX}")
message(STATUS "Building for version: ${VERSION} / ${LIBVER}")

########################################################################
# Build and register unit test
########################################################################
include(GrTest)

# If your unit tests require special include paths, add them here
#include_directories()
# List all files that contain Boost.UTF unit tests here
list(APPEND test_sched_sources
)
# Anything we need to link to for the unit tests go here
list(APPEND GR_TEST_TARGET_DEPS gnuradio-sched)

if(NOT test_sched_sources)
    MESSAGE(STATUS "No C++ unit tests... skipping")
    return()
endif(NOT test_sched_sources)

foreach(qa_file ${test_sched_sources})
    GR_ADD_CPP_TEST("sched_${qa_file}"
        ${CMAKE_CURRENT_SOURCE_DIR}/${qa_file}
    )
endforeach(qa_file)
